export default function Fcfs(req, res) {
    res.status(200).json('#include <stdio. H> int main() { int n, bt[20], wt[20], tat[20], i, j; float avwt, avtat; printf(" fcfs scheduling\n"); printf(" =================\n"); printf("enter total number of processes: "); scanf("%d", &n); printf("\nenter burst time : \n"); for (i = 0; i < n; i++) { printf("p[%d]: ", i + 1); scanf("%d", &bt[i]); } wt[0] = 0; for (i = 0; i < n; i++) { wt[i] = 0; for (j = 0; j < i; j++) wt[i] += bt[j]; } printf("\nprocess\t burst time\twaiting time\tturnaround time\n"); for (i = 0; i < n; i++) { tat[i] = bt[i] + wt[i]; avwt += wt[i]; avtat += tat[i]; printf("p[%d]\t %d\t\t%d\t\t%d\n", i + 1, bt[i], wt[i], tat[i]); } avwt /= i; avtat /= i; printf("\naverage waiting time: %f\n", avwt); printf("average turnaround time: %f\n", avtat); return 0; }')
  }  